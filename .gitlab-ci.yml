stages:
    - build
    - deploy
    
build-docker:
  stage: build
  before_script: 
    - docker rmi -f muchdevops || true
  script:
    - docker build -t muchdevops . 
  tags:
    - build
deploy-docker:
  stage: deploy
  script:
    - docker run -d --name flappymadafucky -p 80:80
  tags:
    - deploy
  only:
    - master